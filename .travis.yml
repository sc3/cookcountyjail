language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: "python -m py.test"

notifications:
  email:
    recipients:
      - supremecoding@googlegroups.com
    on_success: never
