language: python

# versions to test against
python:
  - "2.7"

# set env vars
env: 
  - DJANGO_SETTINGS_MODULE=countyapi.settings PYTHONPATH=''

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: "py.test"
